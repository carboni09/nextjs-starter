
const parameters = {
    incrementListenInterval: 300, //time interval to run incrementSecondsListened f
    minimumSecondsListenedToAchieveDailyGoal: 900, // number of seconds a user must  listen to mark the daily goal as complete
    sermonCompletionPercentage: 0.70 //percntage of the sermon that must be listened to to mark it as completed
}

export default parameters