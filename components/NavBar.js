//import { useDispatch, useSelector } from 'react-redux'
import { OPEN_NAVIGATION } from '../redux/actions/types';
import Link from 'next/link'
export default (props) => {
	//let dispatch = useDispatch()

	return (
		<div className="">

		</div>
	)
};
